# agrodss — Claude Code 작업 지침

`D:\agrodss`. 몰 중심 수익 + 납품 농가용 DSS. 정본 설계는 `docs/` 의 트리 문서다 —
코드는 그 문서를 구현하지, 문서가 코드를 따라가지 않는다.

## 정본 문서

| 문서 | 역할 |
|---|---|
| `docs/design_tree_review_20260918.md` | DSS 트리 평가 · §4 즉시 항목 · §6 몰 중심 재판정 |
| `docs/agrodss_mall_tree_20260918.md` | 몰 트리 |
| `docs/agrodss_milestones_20260918.md` | 시계열 6단계 · 관문 |
| `docs/agrodss_port_inventory_20260918.md` | VELA 외부 API 경험 — **코드 이식 아님** |
| `docs/agrodss_backlog.md` | **작업대장 — 진도의 정본.** 계획(마일스톤)과 어긋나면 대장이 맞다 |

## 작업대장

- 세션 시작: 대장의 `진행`·`대기` 항목을 읽고 묶음 하나를 고른다.
- 세션 마감: 손댄 항목의 상태 열을 갱신하고 완료에는 검증 커밋 해시를 적는다. **갱신 안 한
  완료 보고는 미완이다.**
- 항목 삭제 금지. 등재 문턱 3칸(대장 머리) — 위험 없는 관찰은 등재하지 않는다.
- 근거란에는 측정 시점을 적는다.

## 절대 제약

- **몰↔DSS 경계**: 3층 판단 입력에 재고·주문 잔량·판매 속도·정산액·리뷰 평점·수요 예측이
  들어가지 않는다. 수확량·품질 등급·반품 사유(품질 갈래)·영상은 흐른다. 양방향 검사로 고정.
- **하드코딩 금지**: 수치·키·URL 은 설정 한 곳. 값은 `.env`(gitignore)에만 — 문서·커밋에 기재 금지.
- **대리값 금지**: 기록 공백을 대표값으로 메우지 않는다. 수령 불명이면 되묻는다.
- **인증 표기**: 인증서 없이 유기·무농약 표기 금지 — 안전·법규 판단은 발행자 확인.
- **영상 PII**: 덤프·프롬프트를 파일로 남기지 않는다. 즉시 판독 후 폐기.
- **VELA 코드 참조 — 외부 API 클라이언트만 인용한다(D-9, 발행자 2026-09-18)**: LLM 을
  제외한 외부 원천(기상 · 토양 · 병해충 · 농약/공시자재 · 시세 · 지적 등)의 1층 클라이언트는
  `D:\vela\backend_new\services` 등에서 인용해 `ingest/` 에 다시 쓴다(원본 경로·버전을 파일
  머리에 적는다). **3층 판단 로직 · LLM 조립 · VELA 설정 결합은 가져오지 않는다.** 그 외
  VELA 코드는 참조하지 않는다.
- **외부 배포 금지(개발 중)** — 공개 URL · 도메인 · 클라우드 배포 · 외부 접근 가능한 포트를
  열지 않는다. 해제는 발행자 결정(대장 D-6). 배포 설정 파일을 만들 때도 대상은 localhost 뿐.
- **내부 화면 상시** — 진행하는 대로 화면(frontend)이 localhost 에서 보여야 한다. 마일스톤을
  완료로 갱신하려면 그 산출이 화면에 반영돼 있어야 한다(대장 M-13 관문). 종이 산출도 화면에
  싣는다 — 대장 · 트리 · 스키마가 첫 화면이다.
- **스키마 정본 하나(M-6)** — 레코드 종류·필드·출처는 `schema/records.py` 에만 선언한다. 원장에
  쓰는 코드는 쓰기 직전 `sch.stamp()` 를 거친다. 경계 허용 목록은 스키마에서 파생(사본 금지).
- **자율진화는 보수 방향만 자동(D-14, 발행자 2026-09-19)** — 시스템이 스스로 적용할 수 있는
  개선은 등급 하향 · 판단 불가 전환 · 범위 축소뿐. 임계 · 규칙 · 격자 칸 · 사전 변경(확장)은
  개선 항목으로 **제안까지**, 채택은 사람(farmer · publisher). 사용자 개선 요구는
  `feedback.request` 로 받아 개선 항목이 되고, 반영 뒤 라이브 독립 3회 3/3 이어야 검증이다.

## 파일 수정

- 소스·문서 수정은 **Edit/Write 도구**. heredoc · `python -c "…"` 파일 쓰기 금지
  (VELA 에서 네 번 조용히 실패했다 — `>/dev/null` 이 실패를 삼킨다).
- Windows: 파이썬 자식은 `encoding="utf-8", errors="replace"`. PowerShell `>` 리다이렉트로
  한글 파일을 쓰지 않는다(UTF-16).
- **훅(U-13)**: `.claude/settings.json` PreToolUse(Bash|PowerShell) →
  `scripts/hook_block_shell_authoring.py` 가 heredoc·인라인(-c/-e)·PowerShell 파일 쓰기와
  큰따옴표 인라인 안 백틱·`$(` 를 막는다(읽기 전용 측정은 통과). 래칫 `tests/test_hook_guard.py`.
  **가드 창**: 훅은 세션 시작 시 읽혀 만든 회차에는 안 돈다 — 세션 시작 시 스크래치패드에
  heredoc 파일 쓰기 1회 시도로 활성을 확인한다(차단되면 활성, 통과하면 그 세션은 규율로만 지킨다).

## 검사 규율

- **검사의 검사**: 새 검사는 실패하는 입력(주입)으로 살아있음을 확인한 뒤 배치. 주입은
  원 결함을 완전히 재현해야 한다. `failed` 와 `error` 를 함께 센다.
- **주입 하네스는 바이트코드 캐시를 지운다**(자기 도구 오류 실측 2026-09-20). 파이썬 `.pyc` 는 소스의 (mtime 초 · 크기)로 판별하므로
  **같은 길이의 치환을 같은 초에 두 번** 돌리면 직전 주입의 바이트코드가 재사용된다 — X3 주입은 실행된 적이 없었고 "미적발"은 거짓이었다
  (반대로 직전 주입의 실패가 이월돼 거짓 "적발"도 난다). 매 회 `__pycache__` 삭제 + `PYTHONDONTWRITEBYTECODE=1` + `python -B`.
  하네스 결함이 의심되면 주입본으로 함수를 **직접 호출**해 결과를 본다(그날 그것으로 잡았다).
- **배선을 검증한다**, 함수가 아니라 — 호출부가 안 쓰면 결함 그대로.
- **정본을 세운 회차에 소비자 배선을 전수로 센다**(발행자 제정 2026-09-20). 정본을 세우는 것과 소비자가 그것을 쓰는 것은 다른 일이다 —
  `config.today()` 를 세운 직후 세니 서버→ingest 두 호출이 안 넘기고 있었다(VELA N-147 과 같은 형태, 이 저장소에서만 두 번).
  세는 법: 정본이 대체한 옛 호출형(예: `date.today()`)과 정본을 **받아야 하는 인자**(예: `today=`)를 둘 다 grep 한다.
- **말뭉치는 append-only** — `tests/test_chat_corpus.py` 의 문장은 빼지 않고 기대 종류만 고친다. 래칫의 크기가 줄면 "왜 줄었나"를
  나중에 물을 수 없다(발행자 2026-09-20). 문장 수 하한이 검사에 있고 위로만 올린다.
- **게이트는 거부와 통과를 둘 다 본다.**
- **래칫은 형태 독립** — 호출형까지 고정, 인자 표현식·리터럴·줄 위치·문자 창은 고정하지 않는다.
  구역(함수 본문)을 잘라 그 안에서 본다.
- **좁은 스코프 실행 금지** — 커밋 전 전체 관문. 증분과 신규 검사 수를 대조한다.
- **관문이 도는 동안 소스를 고치지 않는다.**

## 측정 규율

- 실측에는 **측정 시점**을 적는다. 외부 상태(API·쿼터·날씨)에 좌우되는 값은 재사용 전 다시 잰다.
- 결과가 한쪽으로 쏠리면("전부 0" · "전부 해당") **도구를 먼저 의심한다.**
- 처방 직후 **같은 형태를 전수로 센다** — 한 곳만 고치고 닫지 않는다. 같은 형태라도 급을 가른다.
- 라이브 재현은 **독립 세션 3회**, 3/3 이어야 완료. 응답 0초면 캐시를 의심한다.
- 한 증상 뒤에 게이트가 **줄지어** 있는지 본다 — 하나만 열고 "처방이 틀렸다"고 오진하지 않는다.

## 자율 운영

- 확인 요청은 셋뿐: ①안전·법규 ②지시 범위 밖 처방 ③대장 등재. 나머지는 보수적인 쪽을
  택하고 사유를 보고에 남긴다.
- 한 지시는 측정·처방·검증·커밋·관문까지 마치고 보고한다. 백그라운드를 띄우고 턴을
  넘기지 않는다 — 폴링해서 기다린다(상한 통상 소요 2배, 처음이면 5분 후 상태 보고).
- 세션 범위 = 묶음 하나 + 마감.

## 완료 보고

```
- [ ] 관문(전체 검사): N passed · 직전 대비 증분 = 신규 검사 수
- [ ] **발행자 경로 HTTP 걷기**(관문과 별개 항목 — 발행자 등재 2026-09-20): `python -m pytest tests/test_publisher_path_http.py` 결과를
      따로 적는다. 화면·배선을 바꾼 묶음은 걷기에 그 화면의 한 걸음을 더한다(단위 검사는 "봉투는 정상 · 화면도 정상 · 둘 사이가 안 이어짐"을 못 본다)
- [ ] 라이브 3/3 (판단 산출을 바꾼 경우) — `python scripts\live_reproduce.py` 가 독립 프로세스 3회로 재현·대조해
      `verification.live` 원장에 적는다. 외부 원천(키)이 0 이면 '미성립'이고, 2/3 은 잔여다
- [ ] 검증이 이뤄진 커밋 해시
- [ ] git status 잔여를 커밋 메시지와 대조(마감 점검)
- [ ] 대장 갱신: docs/agrodss_backlog.md 상태 열 (손댄 항목 전부)
- [ ] **최선의 다음 한 수** — 후보 나열이 아니라 하나를 고르고 이유 한 줄
```

**라이브 반영(2026-09-19 발행자 "리프레시하면 반영")**: 발행자 PC 의 화면 서버는 `git pull` 로 HEAD 가 바뀌면 스스로 내려가고
`run_frontend.bat` 가 새 코드로 다시 띄운다(`frontend/serve.watch_head` · 종료 코드 3 · `AGRODSS_RELOAD=0` 으로 끔). 그러므로
"재시작해 달라"고 말하지 않는다 — pull 만 부탁하고 화면 우하단 HEAD 로 확인한다. 데이터(등록부 · 원장)는 요청마다 새로 읽는다.
발행자 실측은 `scripts\live_check.bat`(메모장 로그, 값 없이 상태만) — 배치 안에는 ASCII 만, 리다이렉트 블록 안에 괄호 금지
(cmd 가 블록을 닫는다 — 2026-09-19 21:30 빈 로그 실사례, `tests/test_env_and_live_check.py` 가 고정).

### 최선의 다음 한 수 (발행자 제정 2026-09-18)

이벤트 하나(묶음 · 결정 · 처방)가 마감되면 보고의 **마지막 줄은 권고안 중 최선 하나**다.
"할 수 있는 것" 목록은 대장이 이미 들고 있다 — 보고가 할 일은 **고르는 것**이다.

```
고르는 기준   시즌 시계(첫 수확까지)에서 임계 경로에 있는가 → 되돌리기 어려운 것이 먼저 →
              발행자만 할 수 있는 것과 세션이 할 수 있는 것을 가른다
형식          "최선: <한 수>. 이유: <한 줄>. (차선은 대장에)"
```

후보를 셋 이상 늘어놓고 끝내면 미완 보고다.

# 핸드오버 2026-09-19 — 세션 마감 (agrodss main b56d7b4 → de4dc95, 커밋 17)

**한 줄**: 코드로 닫을 수 있는 것은 거의 다 닫혔다(결정 9/9 · 되먹임 양방향 · 원천 인용 다섯 · 채팅 화면 ·
라이브 채비 · 몰 목업). **라이브 실측은 0 이다** — 남은 미확인의 대부분이 키 하나와 기록 한 줄에 걸려 있다.

전체 관문 321 passed(세션 마감 1회 · 검증 트리 de4dc95). 대장 60항목 · 완료 32 · 미결 28(대기 9 · 진행 6 · 보류 13).
운영 데이터(`data/`) 오염 0 — 마감 점검 `git status` 잔여 0.

## 1. 오늘 선 것 (묶음 순서 · 커밋)

| 묶음 | 커밋 | 무엇 | 검사 · 주입 |
|---|---|---|---|
| M-3 경계 | eebac73 | 허용 목록 게이트 1회(입력 수집 뒤 · 판정 앞) · 정산→사건 · 납품 계획일 출처 | 15 · 3/3 |
| M-6 스키마·되먹임 | e784a39 | 레코드 종류 한 정의 · 필지 등록부 · 마이그레이션 · 개선 요구/예측 원장/대조/개선 항목 · 자율진화(보수만 자동, D-14) | 25 · 3/3 |
| M-13 채팅 화면 | bc26247 | Claude 형식 3열 · 목록 = 재배 단위 · 발화 분류→확인→원장 · 영농일지 | 23 · 4/4, **R-4** |
| 기동 셋 | 85020ef · 1b5b4c0 · 57f034b | 파이썬 자동 탐색 배치 · 진단 배치 · markdown 없이도 기동 | 2 |
| 어휘 | 96d8d2d | 발행자 첫 발화 "줄기가 매우 왕성한 모습이다" → 관찰 어휘 · 지역 시각 | 1 |
| U-16 피해 대조 | 40b29fa | 피해 사건(risk 필수) · 경보↔피해: 적중/놓친 경보/과경보(회복 불가는 허용 H) | 14 · 3/3 |
| M-10 결정 등록 | 66bdbaf | 격자 선언 결정 9/9 · 봉투 12 · 양·적기·규칙은 지식 미비로 | 9 · 3/3 |
| M-13 음성·반입·동기화 | 9712361 | 🎤(5초 무신호 종료 · D-15) · 질문/답변 아이콘 · 📎 사진·영상(EXIF/mvhd) · 사용자 탭 · D-16 옵트인 | 16 · 3/3 |
| M-15 ⑥ 비료 처방 | a847853 | 흙토람 FrtlzrUse/StdUse(D-9) · 밑거름 판단함 · 웃거름 양 · 두 코드 체계 분리 | 8 · 3/3 |
| M-15 ⑤ PSIS | 2dccc04 | 관행 갈래 등록약제 + 안전사용기준 · 작용기작 라운드로빈 | 13 · 3/3 |
| M-10 라이브 채비 | ba73e91 | `scripts/live_reproduce.py` — 독립 프로세스 3회 · 미성립(외부 원천 0) · 캐시 의심 | 7 · 2/2 |
| U-14 이름 채집 · AGRODSS 탭 | 9a71648 | 모르는 이름 → 후보 → 발행자 승인 → 정본 CSV · 좌측 상단 AGRODSS 홈 링크 전 화면 | 18 · 3/3 |
| U-15 검색어 정밀도 | 860c8bc | 재측정이 등재 처방을 뒤집음 — 자재명 성분 부분일치, 제품명은 안 본다 | 6 · 2/2 |
| M-11 몰 목업 | de4dc95 | 영상 시계열 상세 · 인증서 없이 표기 금지 · 판정 비노출 · 몰-H 재귀 게이트 | 5 · 2/2(원안 1 미적발 → 교정) |
| (마감 뒤) 키 로딩 | d9e2a01 | CLI 가 `.env` 를 안 읽었다 → `ingest/config.load_dotenv` | 4 |
| (마감 뒤) U-13 훅 | b17716d | 셸 파일 쓰기 차단 PreToolUse(Bash\|PowerShell) — **가드 창**: 만든 회차엔 안 돈다, 다음 세션 시작 시 heredoc 쓰기 1회로 활성 확인 | 34 · 2/2(+기준선 결함 1: node writeFileSync) |
| (마감 뒤) M-5 층 관문 | 3249938 | 다섯 조건이 이미 코드에 있었다(낡음 대조) → 실제 등록부 12봉투 위 관문 | 7 · 3/3 |
| (마감 뒤) 발행자 라이브 3건 | 259fcfd | 사용자 탭 매 페이지 · 요청형 질문("…알려줘요") · **서술문은 내부 로직으로 관찰 메모 제안(분류 안 됨을 사람에게 안 넘김)** · 계획 대 실제 답 순서(지금→예정→놓침) | 12 · 3/3 |
| (마감 뒤) 분류기 전수 | 522e7b7 | 40문장 측정 9건 교정 · 말뭉치 래칫 42 — 이후 어휘 확장은 표에 문장을 넣는 것으로 | 43 · 1/1 |
| (마감 뒤) 코드 평가 | e2c1986 | `docs/code_review_20260919.md` — 정합 중간 · 논리 중간 · 합리 높음 · 상 6 | — |
| (마감 뒤) 평가 처방 ① | 06484c2 | .env 로더 하나(D2) · env_from_vela 빈 KEY= 교체 · live_check.bat ASCII/--subject/지연 확장(D11) | 4 · 1/1 |
| (마감 뒤) 평가 처방 ②③ | 87ef041 | 재배환경 대리값 제거 → 되묻기(C2) · price 금지 필드 제거 + 인용 출구 스키마 검증(C1, /judge 소비자 제거) | 2 · 2/2 |
| (마감 뒤) 평가 처방 ④ | fefd4f1 | 되먹임 폴백 제거(A1) · 예측 창을 마지막 확인일까지(A2 last_seen_at) | 2 · 2/2 |
| (마감 뒤) 평가 처방 ⑤ | 071064d | 수확 지연 영구 경보 — 판정기가 수확 사건·단위 종료를 본다(B1) | 1 · 1/1 |
| (마감 뒤) 평가 처방 ⑥ | 211db2d | 기준점 형식·상태 어휘를 스키마가(D3·A12) · 핸들러 총괄 예외 → 500 페이지(D4) | 3 · 2/2 |
| (마감 뒤) 평가 칸 2 ⓐ 관절 정합 | 3fb88d2 | 게이트가 레코드 필드를 본다(A3) · N/A(A4) · recoverable bool(A5) · gdd 거부(A7) · 필지 R-4(C4) · 원천 셋이 config 하나(C11) | 5 · 6/6 |
| (마감 뒤) 평가 칸 2 ⓑ 결정 논리 | 45c6fb1 | cert 필요 축(B2) · 웃거름 이행 판정 = 계획 대 실제 매처(B3) · 순서(B5) · mall_supply 배선(B7) · 조건부·소급 작업은 놓침 아님(B13) | 5 · 5/5 |
| (마감 뒤) 평가 칸 2 ⓒ 반입 경계 | ce63bf8 | 과거형 월/일 연도(C6) · 재확인 방지(C7) · stamp→이동(C8) · 잘린 mvhd(C9) · 예보 비숫자(C10) · 관측일 없는 예찰(C12) · 사유 날짜(C13) | 7 · 7/7 |
| (마감 뒤) 평가 칸 2 ⓓ 화면·훅 | 61d0ebc | 413(D5) · POST 같은 출처(D6) · 훅 `py -3 -c`/경로/다음 줄 리다이렉트/stdout 통과(D8~D10) · 생성물 수치 계산(D12) — **칸 2 세션 몫 끝** | 14 · 5/5 |
| **첫 라이브 실측** 21:34 | 5c8afa3 | live_check.bat 빈 로그 — 블록 안 괄호가 블록을 닫음(diag 도 같은 형태) | 1 |
| 라이브 회신 처방 | b17814d | 지오코딩 좌표·PNU → 필지 등록부 · `--env` 로 재배환경 저장 · **git pull 이 오면 서버가 스스로 다시 뜬다**(종료 3 → bat 루프) · 관행 그룹 렌더 | 7 |
| C3 표기 | b1bca75 | PSIS 대체 조회를 "쪽파 미등록 · 파 등록분"으로 말한다(존폐는 발행자 결정) | 1 · 1/1 |
| 발행자 답 "노지" | 4e9dddf | 필지 등록부 environment=노지 — 처방 되묻기 해소, `--env` 불필요 | 0 |
| 평가 §1-1 고아·중복 진실 | 4b061a6 전 | 게이트 우회 사문 삭제 · PII 제거 정본 하나 · 평년값 미배선 결정 고정 | 3 · 1/1(첫 판 미적발 — 독스트링 겹침) |
| 발행자 다음 행위(9/8 예찰 한 줄) 걷기 | (이 커밋) | 이행 경로는 선다(사건 예찰 → 계획 대 실제 이행 2). 잡힌 것: "트랩 확인은 안 했다"가 예찰 **사건**(조사가 붙은 부정 — 안 한 것이 한 것으로) · "9/8" 빗금 날짜 미인식 → 둘 다 처방, 걷기에 예찰 한 걸음. **하네스 결함**(같은 길이 치환 + 같은 초 → `.pyc` 재사용, X3 미실행) 발견 → 캐시 안전 하네스로 오늘 주입 36건 재실행 36/36 | 4 · 36/36(재검) |
| 오늘 정본 소비자 래칫 · update.bat · 배치 전수 | 0891438 | "정본 세운 회차의 소비자 배선 전수" 래칫(frontend `date.today()` 0 · 호출 셋이 오늘을 넘긴다) · `scripts\update.bat`(옛 등록부 로컬 수정이면 버리고 pull) · 배치 괄호·ASCII 검사를 고정 목록 둘에서 전수 5로 | 2 · 3/3 |
| U-18 2단계 — 옛 추적 파일 git 밖 | f636545 | 발행자 확인(비공개 · pull · 덮개 생성) 뒤 `data/parcels.json` 을 `git rm --cached`(작업 사본 유지). 검사는 씨앗 사본 + 검사용 가짜 지번 덮개 — 실제 주소를 어디서도 읽지 않는다 | 0 · 1/1 |
| U-18 1단계 — 등록부 두 겹(씨앗/덮개) · U-19 · 규율 둘 | b2d5c10 | 원격 공개 실측 → 추적 파일은 PII 없는 씨앗 `parcels_seed.json`, 이 PC 의 값은 git 밖 덮개(첫 기동이 옛 파일에서 옮긴다). 옛 추적 파일은 안 건드림(수정된 추적 파일을 upstream 이 지우면 pull 이 멈춘다 — 실측). 규율: 정본 세운 회차의 소비자 배선 전수 · 말뭉치 append-only(하한 75). U-19 보고 문서의 낡은 주장 — 대장 페이지 머리·이유를 회차마다 다시 쓴다 | 5 · 5/5 |
| 첫 농가 리터럴 전수 · 문서의 지번 주소 제거 | f2ba45e | 로직 기본값 둘(fertilizer `--parcel/--crop` 기본값 · `DEFAULT_PARCEL`) → 명시 필수 · 등록부 하나일 때만 기본 · 문서 셋의 실제 주소(/doc 화면 PII) 제거 · 래칫 | 3 · 3/3 |
| 명사 어휘만으로는 사건이 아니다 | 9333e87 | 검토 잔여("비료 상태가 안 좋다" → 시비 사건)를 측정으로 닫음 — 사건은 한 일의 표지(과거 어미 ㅆ · 습니다 · 완료 · 날짜)가 있을 때만, 없으면 관찰 메모 | 7 · 3/3 |
| 세션 산출 코드 검토 — 실행 검증 6건 처방 | 3b009b9 | 날짜 있는 발화도 계획표 잇기 · 다른 서술어의 부정은 사건 유지(과거 동사 어휘 + 어간 2글자 창) · 피해 부정 창 축소('수 없' 제외) · 갈래 긍정 우선 · AGRODSS_TODAY 기동 검증 · 확인된 발화의 종류 변경 거부 | 11 · 6/6(첫 판 S2 미적발 — 가드 둘 검사 하나) |
| §7.5 관문의 입력 — 오늘이 send 에 안 닿음 | fa49bfd | 고정을 세운 직후 세니 서버→ingest 호출 두 곳(send · choose_kind)이 오늘을 안 넘겼다 → 배선 + 걷기 날짜 단언 | 0 · 2/2 |
| live_check 뒤 판독 걷기 · 오늘 고정 | 2517998 | 처방 정본 있을 때 웃거름 카드의 양(추비 N 7.6 · K₂O 5.5) 까지 걷기에 · 걷기가 실제 날짜라 9/25 부터 거짓 실패할 형태 → `AGRODSS_TODAY` 고정(`config.today()` 정본 하나 · 고정 중이면 화면이 말한다) | 0 · 3/3 |
| 발행자 경로 HTTP 걷기 → `/judge` 표현 층 | 84c578e | 지시한 경로를 HTTP 로 걷는 검사 → `/judge` 의 M-10 '판단함' 카드가 배지·축 표뿐(요약 · 양 · 사유 · 경보 미표시 · 제목 원 id)이던 것 적발 · 수정 | 1 · 3/3 |
| §7.5 전수 — 피해 부정 | c421486 | 사건 부정을 고친 직후 같은 형태를 피해 갈래에서 세니 13문장 중 7건이 피해 **사건** → 피해 없음 관찰(경보↔피해 대조 오염 경로 차단) | 10 · 2/2 |
| 불이행 사유 분류 · 사용자 메뉴 | a52f137 | 발행자 "웃거름 주지 않고 …"가 시비 **사건**으로 읽힘 → 사건 어휘 뒤 부정 = 불이행 사유(작목 공통 · 계획표에서 작업명·계획일) · 초안 단위 확인 · 3층 '사유 기록됨' 마감 전에도 · 사용자 탭 = 계정 메뉴 + `/changes`(실행 중 코드 = HEAD) | 18 · 8/8(첫 판 F 미적발 — 목록 자체를 기대로 씀) |

관문 증분 전수 일치: 160 → 175 → 200 → 223 → 225 → 239 → 248 → 264 → 272 → 285 → 292 → 310 → 316 → 321 → 325 → 359 → 366 → 378 → 421 → 425 → 427 → 429 → 430 → 433 → 438 → 443 → 450 → 464 → 465 → 472 → 473 → 473 → 476 → 496 → 506 → 507 → 507 → 507 → 518 → 525 → 528 → 528 → 533 → 533 → 535 → 539.

## 2. 발행자 몫 — 지금 (이 순서로)

**0. 오늘(U-18) — 2026-09-20 발행자 확인으로 ①②③ 끝**: 비공개 전환 · pull · 덮개 생성 확인 → 세션이 옛 추적 파일을 git 에서 뺐다(2단계 커밋).
다음 pull 이 "commit or stash" 로 멈추면 `git checkout -- data\parcels.json` 뒤 다시 pull(값은 덮개에 있으니 잃는 것 없음) — 또는 그냥
`scripts\update.bat` 더블클릭(그 일을 대신 하고 pull 한다). 남은 것은 ④ 이력 정리뿐.
(아래는 그 전 지시문 — 기록으로 남긴다.) 원격 `ParkByunggak/agrodss` 가 **공개** 저장소였다(2026-09-20 실측) — 지번 주소가 9/18 이후 이력에 나가 있다.
① GitHub 저장소 Settings → Danger Zone → **Change visibility → Private**(설정 한 번, 지금 바로). ② `git pull origin main` → 서버가 다시 뜨며
`data\parcels_local.json`(git 밖 덮개)이 자동으로 생긴다 — 옛 `data\parcels.json` 의 주소·좌표·PNU 가 거기로 옮겨진다. 이 커밋은 옛 파일을
**건드리지 않았다**(로컬에서 수정된 추적 파일을 upstream 이 지우면 pull 이 멈춘다 — 실측). ③ 덮개가 생긴 것을 확인한 뒤
`git checkout -- data\parcels.json` 으로 로컬 수정을 버리고 세션에 말한다 → 세션이 옛 파일을 `git rm --cached` 로 빼는 커밋을 낸다(그때부터
그 파일은 .gitignore 대상). ④ 이력에서 지우는 것(force-push)은 파괴적이라 발행자만 결정한다 — 비공개면 급하지 않다.
**한 번에**: `git pull origin main` 뒤 탐색기에서 `scripts\live_check.bat` 더블클릭 → 메모장 내용을 세션에 붙인다.
(① `D:\vela\.env` 에서 키를 이름만 보고 옮김 ② p001 검정값 · 처방 수집 — 상태만 출력 ③ 라이브 3회 재현). 손으로 하면:

1. `.env` 에 키(토양 · VWorld · 비료 · 기상 · 예찰 · PSIS). 이름은 `.env.example`. `python scripts\env_from_vela.py` 가 옮겨 준다.
2. `python -X utf8 -m ingest.fertilizer --subject=p001-jjokpa-2026f --env=노지 --summary` — 작목·주소는 등록부에서 푼다(배치에 한글·주소 리터럴 없음).
   `--env` 는 되물은 답(노지|시설)을 등록부에 남긴다 — **첫 실측(21:34)에서 처방이 no_code 였던 이유가 이것**. 값은 `data/soil/`, 화면엔 상태만.
3. `python scripts\live_reproduce.py` — 출력(3/3 · 캐시 의심 · 다름)을 세션에. 첫 실측: 12/12 일치 · 미성립(외부 원천 0) — 좌표가 등록부에 없어서.
   b17814d 부터 지오코딩 좌표가 등록부에 남으므로 다음 회차에 예보가 붙는다.
4. 재시작은 b17814d 뒤 **한 번만** 수동(Ctrl+C → `.\run_frontend.bat`). 그 뒤로는 `git pull` 만 하면 서버가 HEAD 변화를 보고 스스로 다시 뜬다
   (화면 우하단 HEAD 로 확인 · 브라우저는 새로고침만). 데이터는 원래 새로고침으로 반영된다.
5. 쪽파 채팅에 **9/8 예찰(트랩)** 한 줄 · **9/16 웃거름** — 발행자가 세션에 말한 문장("쪽파 포장에는 웃거름 주지 않고 수분공급만 표면이 마르지
   않게 해 줌. 그 근거는 토양검증 상태를 기준으로 함")을 `git pull` 뒤 **채팅에 그대로** 넣는다 → 초안 둘(불이행 사유 '웃거름 1회' 2026-09-16 ·
   관찰 메모)이 뜨고 각각 '확인 → 원장'. 그러면 판단 화면의 웃거름 1회가 '미이행'에서 **'사유 기록됨'** 이 된다. 세션에 말한 것은 원장에
   안 들어간다 — 원장은 화면에서 확인한 것만. 9/24 가 예찰 마감.
6. 결정: **D-16** 휴대폰 동기화(권고 켠다) · D-2 판정 노출 · D-3 예약 판매.

## 3. 발행자에게 고지한 한계

- D-15: 크롬 음성 인식은 구글 서버를 거친다. 앱은 localhost 뿐이지만 말한 내용은 밖으로 나간다.
- 휴대폰 마이크·음성은 https/localhost 에서만 열린다 — 평문 Wi-Fi(D-16)에서는 글·사진·영상까지.
- 유기 갈래의 시비량 N·P·K 는 **목표 양분량**이다 — 공시 자재로 환산하는 규칙은 정본이 없다(지식 미비 병기).
- 첫 농가가 유기라 관행 갈래(PSIS)는 화면에 안 뜬다.
- 음성 5초 종료 · 사진 EXIF 등 브라우저·파일 의존 동작은 라이브에서만 확인된다.

## 4. 규율에서 나온 것

- **R-4** 테스트가 운영 `subjects.json` 에 33줄을 썼다 — 경로를 기본 인자에 묶어 격리가 안 닿았다. 되돌리고 env 로 풀었다. 이후
  등록부 넷(재배 단위 · 사용자 · 정본 CSV · 토양/PSIS/이름 원장)에 같은 처방을 전수 적용했다(§7.5 지점 축).
- **주입 미적발 1건**(M-11): "공개 뷰 대신 원장 레코드"는 클립 투영이 이미 막아 원 결함을 재현하지 못했다 → 투영을 뚫는 형태로 교정.
  "주입했는데 통과"의 1번(불완전 주입)의 실사례.
- **U-15**: 등재 처방 '정확 일치'가 재측정으로 뒤집혔다 — 근거란은 측정 시점 기록이다. 실측 오탐은 전부 제품명-only 였다.
- **D-14**: 자율진화의 자율 범위 = 보수 방향만. 확장(임계 · 규칙 · 칸 · 사전)은 제안까지, 채택은 사람. 스키마와 상태 전이가 둘 다 막는다.

## 5. 다음 세션 (지시하면 이 순서로)

-1. **코드 평가 `docs/code_review_20260919.md`** — 상 6건의 처방 순서(§4 칸 1)가 첫 수확 전 작업 목록이다. 등재 여부는 발행자 판단(다리 B).
   발행자 확인 대상 셋: C3 PSIS 파→쪽파 대체 인용(법규) · D7 인증 유형 표기(법규) · D1 저장소 안 필지 주소(PII).
-0.5. **live_check 2회차(2026-09-20 05:17 KST · HEAD 8de761d) = 라이브 3/3 성립** — 처방·표준 success(저장 3 · 비료 키는 DATA_GO_KR 공통 키 대체) ·
   외부 원천 있음 · 3회 1.5~2.3초 · 결정 일치 12/12 · 캐시 의심 0 · 원장 `liv_20260919T201719`. 1회차(21:34) 미성립의 원인(좌표 미저장 ·
   재배환경 미상)은 b17814d · 4e9dddf 로 닫혔고 실측이 그것을 확인했다. M-10 완료. 다음 세션의 첫 일은 **발행자 화면의 실제 판정값 판독**
   (밑거름·웃거름이 이제 양을 낸다 · 경보가 신호형이 됐다)과 9/24 전 사건 두 줄.
-0.2. **발행자 판정 2026-09-20(평가문 대조 뒤)** — 9/20 문장: *"코드는 초록이되 걸을 때마다 결함이 나오고, 라이브는 섰고, 격자 지식은
   0이며 그것을 열 외부 통로는 없다."* 등재 둘: **U-17** 발행자 경로 HTTP 걷기를 마감 절차에(관문과 별개 — CLAUDE.md 완료 보고에
   들어감) · **U-18** PII 사본 증식(병목 셋과 별도 항목). 판정 하나: **첫 시즌 내내 격자 등급 '추정'이 정상 상태** — 격자 지식의 외부
   정본은 없고 검토지는 닫힌 고리, 두 번째 농가만이 연다(m10 §에 고정). 근거 없이 올리지 않는다. 가장 급한 것은 **9/24 마감** —
   예찰·웃거름이 지나면 사유를 묻는 쪽으로 넘어가고, 사유가 기록되면 되먹임 두 번째 칸의 첫 실데이터가 된다("안 한 것도 데이터").
0. **훅 활성 확인**(U-13 가드 창): 스크래치패드에 heredoc 파일 쓰기 1회 시도 — 차단되면 활성, 통과하면 그 세션은 규율로만 지킨다.
1. 발행자 실측 출력(라이브 3/3 · 분류 결과가 틀린 문장 · 처방값)을 먼저 본다 — 그것이 다음 처방의 재료다. 분류 안 됨은 이제 빈 발화에만
   남으므로, 볼 것은 "틀린 종류로 제안됐다"(관찰 메모로 간 사건·계획 등)이다. 발행자 화면의 실행 커밋(HEAD 표시)을 같이 적는다(드리프트).
2. D-16 이 켜지면 휴대폰 첫 접속 확인(토큰 · 401 · 사진 반입).
3. 분류 안 됨 어휘 확장(발행자 문장을 검사에 고정). 2026-09-20 발화 ④(부정 → 불이행 사유)는 닫혔다 — 다음 발화가 틀린 종류로
   제안되면 `tests/test_chat_corpus.py` 에 넣는다(52문장). 발행자 화면에서 "웃거름 주지 않고 …"를 확인한 뒤 `/judge` 의 웃거름 1회가
   '사유 기록됨'인지가 첫 확인이다. 사용자 탭 메뉴(스크린샷 형식)와 `/changes` 는 발행자가 열어 보고 빠진 항목을 말하면 `USER_MENU` 에 더한다
   (실재 경로만 — 언어 · 팀 · 로그아웃은 없어서 안 넣었다).
   (검토 잔여 "비료 상태가 안 좋다 → 시비 사건"은 측정으로 닫았다 — 사건은 한 일의 표지가 있을 때만. "방제 완료" · "오늘 파종"은 완료·날짜
   표지로 사건에 남는다. 남은 형태는 과거 어미가 있는 상태 서술("비료 포대가 남았다")뿐이며 드물다 — 발행자가 실제로 치면 그때 넣는다.)
4. M-11 다음 단계는 D-2 · D-3 · D-8 결정 뒤 — 영상 저장/스트리밍은 자체 구축 금지 영역.
5. 대장 순증: 오늘 등재 +6 · 해소 +8. 3주기 연속 순증 >0 은 아니다.

## 6. 정본 위치

대장 `docs/agrodss_backlog.md` · 스키마 `schema/records.py`(생성물 `docs/schema_records.md`) · 설계 `docs/m6_schema_feedback.md` ·
`docs/m13_chat_screen.md` · `docs/m10_harvest_timing.md` · `docs/m11_mall_mockup.md` · 규율 `CLAUDE.md`.
발행 대장 페이지: 세션 아티팩트(Version 24 = de4dc95 + 이 핸드오버).

# M-10 ① — 수확 시기 (첫 3층 결정 · M-8 첫 등록)

**무엇을 지키려는가**: DSS 가 이 시즌에 내는 첫 값이 I-1 봉투 형식으로, 판별 순서를 그대로 밟아,
쓴 축과 등급과 상한 제약을 달고 나온다. 화면은 봉투만 받는다.

작성 2026-09-18. 대장 M-10 · M-8. 코드: `judge/registry.py`(등록) · `judge/harvest_timing.py`(판정) ·
`judge/run.py`(원천 수집) · 화면 `/judge`.

## 등록 (F 3층 등록 조건)

| 항목 | 값 |
|---|---|
| id | `harvest_timing` |
| 필요 축 | `anchor` — L "정식일·품종만으로 성립" |
| 선택 축 | `temp` · `gdd` · `forecast` — 있으면 등급이 오르고 상한 제약이 붙는다 |
| 금지 축 | `humidity_air` |
| 판정 규칙 | 기준점 + 격자 수확 창(from~to 일). 품종 미확인이면 범위 제시. 예보 최저기온 < `frost_tmin_c` 인 날이 창 안이면 상한 제약(첫 서리). 등급 = 쓴 축과 격자 출처 중 가장 약한 것 |
| 재판정 주기 | 7일 (예보가 바뀌면 답이 바뀌는 게 정상) |
| params | `frost_tmin_c = 0.0` — 격자 위험 트리거 문면(영하). 쪽파 내한 한계는 미채움이라 **보수적 대체값, 발행자 검토 대기** |

등록부는 규칙·필요 축·금지 축 중 하나라도 없으면 등록을 거부한다. 결정의 필요 축은 격자 칸의
판정 축 안에 있어야 하고, 칸의 금지 축은 결정도 금지해야 한다(검사).

## 판별 순서 (I-1 §3 그대로)

```
해당하는가          격자 단위 · 수확 칸 없음 → 해당 없음
격자 칸 채워졌는가   window 없음 → 판단 불가(지식)   ← 데이터보다 먼저
필요 축 있는가       anchor 없음 → 판단 불가(데이터) missing=[anchor, 농가]
                    → 판단함
```

## 2026-09-18 첫 산출 (쪽파 × 가을, T=08-25)

```
kind        판단함
창          2026-10-14 ~ 2026-11-03 (중심 10-24, ±10일)
등급        추정   ← 격자가 추론 초안이라 여기서 막힌다. 격자 검토(⑤)가 끝나야 '관측'으로 오른다
쓴 축       anchor 만. forecast·temp 없음(좌표·키 미투입)
상한 제약    없음(예보 없음)
재판정       2026-09-25
소비자 노출  아니오 (D-2)
```

**등급이 '추정'인 이유가 데이터가 아니라 지식**이라는 점이 이 산출의 요지다. 키를 넣어도
격자가 검토되기 전에는 등급이 오르지 않는다.

## ② 달력형 위험 경보 (`judge/risk_alert.py`, 등록 `risk_alert`)

| 항목 | 값 |
|---|---|
| 필요 축 | `anchor` · 선택 `forecast` `temp` `precip` · 금지 `humidity_air` |
| 대상 | 오늘이 속한 칸 + `horizon_days`(7) 안에 시작하는 칸의 위험 전부 |
| 비대칭(H) | **회복 불가**: 창이 열리면 신호 없이도 `주의`(달력, 오경보 감수) · 신호가 임계를 넘으면 `경보` · 아직 안 열렸으면 `예고`. **회복 가능**: 신호가 임계를 넘을 때만 `경보`, 그 외 침묵 |
| 신호(예보) | 서리: 최저 < `frost_tmin_c`(0℃) · 과습: horizon 강수 합 ≥ 50mm · 잎 젖음 대용: 강수확률 ≥70% 3일 연속 |
| 수확 지연 | 수확 창을 넘기면 `경보`(회복 불가) |
| 재판정 | 1일 |
| 임계 출처 | 전부 격자 위험 트리거 문면에서 온 **보수적 대체값** — 쪽파 정본 임계는 미채움, 발행자 검토 대기 |

2026-09-18(T+24) 산출: 판단함 · 등급 추정 · `주의` 고자리파리 유충(3단계 창 안, 예보 없음) ·
`예고` 과습·뿌리 부패(4단계가 6일 뒤) · 회복 가능 3건(총채·좀나방·노균병)은 신호 없어 침묵.

### 예찰 신호 (M-15 ③, 2026-09-18 연결)

`pest_regional` 레코드(NCPMS SVC51)에서 **위험의 병해충명과 맞고**(고자리 · 총채/좀나방 · 노균 · 녹병/잎마름)
수준/본문에 **발생·주의·경보·다발·증가** 중 하나가 있으면 신호. 회복 불가는 '주의'→'경보', 회복 가능은
침묵→'경보'. 쪽파는 NCPMS 코드 미확정이라 **'파' 대리 작물**로 조회하며 근거 문장에 그 조건이 붙는다.
응답 필드명은 실응답으로 확정 전(원문 raw 보존) — 첫 라이브 호출 뒤 `_F` 별칭표를 실측으로 고친다.

## 자재 인용 — 첫 '사실 인용' (`judge/material_citation.py`, M-15 ④)

I-1 §2-3 의 첫 사례. 판단하지 않는다 — 오늘 칸의 유기 자재 계열(격자 문면 → 검색어)마다 농관원 공시
목록에서 유효 제품을 인용하고, 출처(15080748) · 목록 시점(fetched_at) · "효능 보증 아님"을 함께 싣는다.
cert=유기만 대상, 관행은 해당 없음(PSIS 인용 M-15 ⑤ 전). 목록에 없는 계열은 no_data — 범주명으로 메우지 않는다.
2026-09-18 산출: 3단계 계열 5(BT·님·유황·석회보르도액·유기질 비료) 전부 인용 성공(유효 10·57·90·17·119건).
검색어 부분일치의 잡음(U-15)은 등재.

### 관행 갈래 — PSIS 등록약제 (M-15 ⑤, `ingest/psis.py`, 2026-09-19)

`cert=관행` 이면 오늘 칸의 **병해충 위험**(격자 risks 이름 → 검색어: 고자리파리 · 파총채벌레 · 파좀나방 ·
노균병. 서리 · 부패 · 지연은 대상 아님)마다 PSIS 농약등록정보(SVC01)를 인용한다 — 품목 · 상표 · 용도 ·
희석배수 · 안전사용시기 · 사용횟수 · 작용기작. VELA 규칙 둘을 그대로 가져왔다: 필드 화이트리스트(제조
구분을 상표로 오독한 실사례) · 작용기작 라운드로빈(같은 기작만 5종 나열되던 실사례).

```
키 없음   판단 불가(데이터) — PSIS 전용 활용신청 키(.env PSIS_API_KEY)
0건       no_data 그대로(범주명으로 메우지 않는다). 정본명 0건이면 표기 후보('파')로 한 번 더 — queried_as 에 남긴다
캐시      data/psis/(git 제외) · 7일 — 등록정보는 저변동
```

인용문 note: *등록 = 그 작물·병해충에 쓸 수 있음(PLS). 안전사용기준을 지킨다. 효능 보증 아님. 유기·무농약
인증 필지에는 쓸 수 없다.* 첫 농가(유기)에서는 이 갈래가 안 뜬다 — 관행 농가가 들어올 때 라이브 확인.

## ③ 표준 방제력 + 계획 대 실제 (`judge/plan.py` · `ingest/events.py` · `judge/plan_vs_actual.py`, 2026-09-19)

| 조각 | 내용 |
|---|---|
| 계획(1층 계획 데이터) | 격자 작업 칸 + 기준점 → 작업일 · 준비 착수일(자가/임대) · 마감일 · 인증 갈래 자재. 촬영 칸도 계획. `source=computed:grid` |
| 사건 원장(I-3 §2) | 파종·정식·방제·시비·관수·제초·예찰·보식·배수·수확·납품·저장·소독·정리. 대상 시각 없으면 거부. 격리 `AGRODSS_EVENTS_DIR` |
| 불이행 사유(I-3 §5) | 계획 작업명·작업일·사유. "안 따른 이유가 조언보다 값지다"(J) |
| 대조(결정 `plan_vs_actual`) | 이행(작업일±3일 안 같은 종류 사건 · 파종=기준점 · 촬영=영상 원장) / 예정 / 미이행(마감까지 N일) / 놓침(사유를 묻는다) / 사유 기록됨. 준비 착수일이 지났으면 선제 발화 |

2026-09-19(T+25) 산출: 파종 이행(기준점) · 예찰(9/8, 마감 9/24) 미이행 5일 남음 · 촬영 3단계 예정(영상 없음) ·
나머지 예정. 화면 `/events` 에서 사건·사유를 적으면 다음 새로고침에 대조가 바뀐다.

## 결정 등록 — 격자가 선언한 나머지 8건 (`judge/stage_decisions.py`, 2026-09-19)

격자 칸이 선언한 결정 9 중 `harvest_timing` 만 등록돼 있었다. 나머지 8 을 규칙·축과 함께 등록했고,
검사가 *"격자가 선언한 결정은 전부 등록부에 있고 축이 맞는다"* 를 고정한다. 값을 지어내지 않는다 —
정본이 없는 것은 지식 미비, 입력이 없는 것은 데이터 미비.

| 결정 | 칸 | 규칙 요지 | T+25(2026-09-19) 첫 산출 |
|---|---|---|---|
| `sowing_window` | 1 | 기준점 상대 창을 날짜로. 파종 전 목록엔 달력 기준 적기 정본 없음 | 해당 없음(이미 파종) |
| `base_fertilization` | 1 | 창 안에서 soil_chem 없으면 데이터 미비, 있어도 시비량 처방 정본 없어 지식 미비 | 해당 없음(창 지남) |
| `replant` | 2 | 창 안 + 농가 관찰(결주·듬성)이 있어야 판단함. 관찰이 최종 심급 | 해당 없음(창 밖) |
| `pest_alert` | 3 | risk_alert 봉투를 칸 3 으로 잘라 낸다(두 벌 로직 금지) | 판단함 · 주의 고자리파리 유충 |
| `top_dressing_1` | 3 | 작업일·마감·시비 사건으로 이행/미이행/예정. 자재는 인증 갈래. **양은 지식 미비** | 판단함 · 미이행(작업일 9/16, 마감 9/24) |
| `top_dressing_2` | 4 | '필요 시' 판정 규칙이 격자에 미채움 | 판단 불가(지식) |
| `drainage_alert` | 4 | risk_alert 봉투를 칸 4 로 | 판단함 · 예고 과습·부패 |
| `ship_or_store` | 5 | 자가·시험(D-8)이면 해당 없음. 납품 계획일 없으면 데이터 미비. 있으면 저장 일수 + 63일 상한 | 해당 없음(D-8) |

채팅 질문은 구체 결정으로 먼저 간다("웃거름 줘야 하나?" → `top_dressing_1`). 예측 원장에 싣는 주장은
여전히 넷(수확 창 · 경보 · 놓침)뿐이다 — 새 결정의 대조 규칙은 필요해질 때 붙인다.

### 시비량 처방 정본 (M-15 ⑥, `ingest/fertilizer.py` · `ingest/soil_store.py`, 2026-09-19)

밑거름·웃거름의 **양**이 지식 미비로 남아 있던 것을 D-9 인용으로 채운다. 원천은 둘이고 **작물코드
체계가 다르다**(VELA 실측: 쪽파 노지 FrtlzrUse=07027 vs FrtlzrStdUse=07014 — 혼용 금지).

| 원천 | 무엇 | 해상도 | 결정이 쓰는가 |
|---|---|---|---|
| 흙토람 FrtlzrUse `getSoilFrtlzrExamInfo` | 필지 검정값 기반 기비·추비 N·P₂O₅·K₂O + 퇴비(kg/10a) | parcel | **쓴다** — `base_fertilization` 판단함, `top_dressing_*` 양 |
| 흙토람 FrtlzrStdUse `getSoilFrtlzrQyList` | 작물 표준 시비량 | national | **안 쓴다** — 필지값이 아니다. 대조·표시용으로만 저장 |

```
python -m ingest.fertilizer "충청북도 괴산군 연풍면 갈금리 50" --parcel=p001 --crop=쪽파
  주소 → PNU(VWorld) → 토양검정(SoilExam) + 처방(FrtlzrUse) + 표준(FrtlzrStdUse) → data/soil/ (git 제외 · PII)
  success 인 것만 저장한다 — 실패를 정본으로 남기지 않는다
```

실행부는 저장소에서 검정값을 `soil_chem` 으로 붙이고(값은 봉투에 안 실린다) 처방 레코드를 경계
게이트에 통과시켜 결정에 넘긴다. **유기 갈래**에서 N·P·K 는 화학비료가 아니라 **목표 양분량**이다 —
공시 유기질 비료·퇴비로 환산하는 규칙은 정본이 없어 지식 미비로 병기한다(사람이 자재 성분표로 환산).

키: `.env` 의 `FERTILIZER_API_KEY` · `FERTILIZER_STD_API_KEY`(VELA 이름), 비면 `DATA_GO_KR_API_KEY`.
라이브 미확인 — 키 투입 뒤 첫 실측이 발행자 몫이다.

## 아직 안 하는 것

- GDD 기반 창 이동(basis=gdd) — 격자가 anchor 기준이라 지금은 없다. 관측 온도가 쌓이면 M-10 ①' 로.
- 평년 대비 차이(A1 외부 기준) — 평년 레코드는 준비됐고 소비자는 다음 결정에서.
- 라이브 3회 재현 — 키 미투입. 키가 들어오면 `python scripts\live_reproduce.py` 로 잰다(2026-09-19 채비):
  회차마다 **새 프로세스**(같은 프로세스 재호출은 캐시 재판정 1회 — N-152), 결정마다 (종류 · 등급 · 주장) 대조,
  외부 원천이 하나도 안 붙으면 '미성립(외부 원천 0)', 응답 시간이 200ms 아래면 캐시 의심 표지, 결과는
  `verification.live` 원장에 남고 `/improve` 에 보인다. 2/3 은 잔여(완료 아님).
